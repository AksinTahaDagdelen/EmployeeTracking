@model List<EmployeeTracking.Web.Models.Domain.Project>

<div class="container">
    @if (Model != null && Model.Any())
    {
        <div class="container">
            <div class="row justify-content-center">
                <div class="col-md-6 text-center mb-2">
                    <h2 style="font-family: 'Kanit', sans-serif;" class="heading-section text-white">PROJECTS</h2>
                </div>
            </div>
        </div>
        <table class="table table-dark text-dark">
            <thead>
                <tr class="company-heading table-title">
                    <th></th>
                    <th>Project Name</th>
                    <th>Status</th>
                    <th>Operations</th>
                </tr>
            </thead>
            <tbody>
                @foreach (var company in Model)
                {
                    <tr class="table-grown table-title">
                        <td>
                            <div class="img img-hover" style="background-image: url(@company.ImgUrl);"></div>
                        </td>
                        <td>@company.Name</td>
                       @* <td>
                            @foreach (var project in Model)
                            {
                                
                          

                            }
                        </td>*@
                        <td class="status"><span class="active">Active</span></td>

                        <td>
                            <a type="button" asp-controller="ManagerProject" asp-action="Edit" asp-route-id="@company.Id"><span aria-hidden="true"><i style="color:red" class="fa fa-edit ps-5"></i></span></a>

                        </td>
                    </tr>
                }

            </tbody>
        </table>
        @*<section class="ftco-section">
    <div class="container">
    <div class="row justify-content-center">
    <div class="col-md-6 text-center">
    <h2 style="font-family: 'Kanit', sans-serif;" class="heading-section text-white">PROJECTS</h2>
    </div>
    </div>
    <div class="row">
    <div class="col-md-12  ">
    <div class="table-wrap project-list">
    <table class="table table-responsive-xl">
    <thead >
    <tr class ="project-headings table-title">
    <th>&nbsp;</th>
    <th class="project-name">&nbsp;Name</th>
    <th>Status</th>
    <th>&nbsp;</th>
    </tr>
    </thead>
    <tbody >
    @foreach (var project in Model)
    {
    <tr class="alert table-grown table-title" role="alert">
    <td>

    </td>
    <td class="d-flex align-items-center">
    <div class="img" style="background-image: url(@project.ImgUrl);"></div>
    <div class="pl-3 email">
    <span class="span-project-name ">@project.Name</span>
    </div>
    </td>
    <td class="status"><span class="active">Active</span></td>
    <td>
    <a type="button" asp-controller="ManagerProject" asp-action="Edit" asp-route-id="@project.Id">
    <span aria-hidden="true"><i style="color:red" class="fa fa-edit pe-3"></i></span>
    </a>

    </td>
    </tr>

    }


    </tbody>
    </table>
    </div>
    </div>
    </div>
    </div>
    </section>*@
        @* <table class="table table-primary table-bordered table-striped table-hover">
    <thead>
    <tr>
    <th>Id</th>
    <th>Name</th>
    <th>Operation</th>
    </tr>
    </thead>

    <tbody>
    @foreach (var project in Model)
    {
    <tr>
    <th>@project.Id</th>
    <th>@project.Name</th>
    <th> <a class="btn btn-light" asp-controller="ManagerProject" asp-action="Edit" asp-route-id="@project.Id">Edit</a></th>
    </tr>

    }
    </tbody>
    </table>*@

    }
    else
    {
        <h1>There is no spoon :)</h1>

    }
</div>
